import { createStyles } from "@mantine/core";

export const useStyles = createStyles({
  input: {
    width: "95%",
  },
  inputContainer: {
    marginBottom: "10px",
    width: "80vw",
    position: "fixed",
    bottom: 0,
  },
});