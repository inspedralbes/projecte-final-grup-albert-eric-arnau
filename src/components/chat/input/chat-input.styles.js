import { createStyles } from "@mantine/core";

export const useStyles = createStyles({
  input: {
    width: "80%",
  },
  button: {
    width: "20%",
  },
  inputContainer: {
    marginBottom: "10px",
    width: "100%",
    bottom: 0,
  },
});
