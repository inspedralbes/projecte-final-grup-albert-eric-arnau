# Firebase
 
  - Groups(Collection) 
    - GroupID(Document) 
      1. Fields(
        name : string,
        password : string,
        description : string,
        members : array,
        limit : number,
        admin : string,
        imgLink : string,
        )
      2. Messages(Collection)
          - Fields(
            user : string,
            message : string
            ,time : timestamp
            )

# Labs
